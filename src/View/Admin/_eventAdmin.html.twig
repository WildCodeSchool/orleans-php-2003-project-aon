 <section class="cookies container-fluid">
        <div class="row">
            <h2>Gestion des évenements</h2>


            <table class="table table-stripped">
                <thead>
                <tr>
                    <th>Evenement</th>
                    <th>Date</th>
                    <th>Lieu</th>
                </tr>
                </thead>
                <tbody>
                {%  for id in event %}
                <tr>
                    <td>{{ id.title }}</td>
                    <td>{{ id.date }}</td>
                    <td>{{ id.location }}</td>
                    <td><a class="mini activity-button btn btn-primary btn-lg" href="showEvent/{{ id.id }}">Modifier</a>
                    </td>
                    <td>
                    </td>
                </tr>
                {% endfor %}

                </tbody>
            </table>
        </div>
    </section>
